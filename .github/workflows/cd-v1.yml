name: MME V1 CD
on:
  workflow_call:
    inputs:
      service:
        required: true
        type: string
      image:
        required: true
        type: string
  workflow_dispatch:
    inputs:
      service:
        description: Service to deploy (frontend, backend, or ai)
        required: true
        type: choice
        options: [frontend, backend, ai]
      image:
        description: Verified ECR URI with full commit SHA or digest
        required: true
        type: string
permissions:
  contents: read
  id-token: write
concurrency:
  group: mme-v1-production
  cancel-in-progress: false
jobs:
  deploy:
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    environment: production
    steps:
      - uses: actions/checkout@v4
      - uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.MME_AWS_DEPLOY_ROLE_ARN }}
          aws-region: ap-northeast-2
      - name: Validate deployment inputs and prepare SSM command
        env:
          INSTANCE_ID: ${{ vars.MME_EC2_INSTANCE_ID }}
          SERVICE: ${{ inputs.service }}
          IMAGE: ${{ inputs.image }}
        run: python3 scripts/ssm-command.py > /tmp/deploy-command.json
      - name: Deploy through SSM
        env:
          AWS_PAGER: ''
        run: |
          set -euo pipefail
          command_id=$(aws ssm send-command --cli-input-json file:///tmp/deploy-command.json --query Command.CommandId --output text)
          echo "SSM command: $command_id"
          python3 scripts/wait-ssm.py "$command_id" '${{ vars.MME_EC2_INSTANCE_ID }}'
